#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <set>
#include <list>
using namespace std;
#include<deque>
#include <queue>
int main()
{
    queue<int> myVec;
    myVec.(myVec.end());
    myVec.push(10);
    myVec.pop();

    
    deque<int> myList;
    myList.push_back(11);
    myList.pop_back();
    myList.push_front(12);
    myList.pop_front();
// Declaration and initialization of a max-heap (default behavior)
    std::priority_queue<int> maxHeap;

    // Declaration and initialization of a min-heap (custom comparison function)
    std::priority_queue<int, std::vector<int>, std::greater<int>> minHeap;
    minHeap.

    return 0;
}